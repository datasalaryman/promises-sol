/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PROMISESPRIMITIVE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const FULFILL_SELF_PROMISE_DISCRIMINATOR = new Uint8Array([
  191, 163, 223, 59, 105, 132, 4, 68,
]);

export function getFulfillSelfPromiseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FULFILL_SELF_PROMISE_DISCRIMINATOR
  );
}

export type FulfillSelfPromiseInstruction<
  TProgram extends string = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountPromiseAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountPromiseAccount extends string
        ? WritableAccount<TAccountPromiseAccount>
        : TAccountPromiseAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FulfillSelfPromiseInstructionData = {
  discriminator: ReadonlyUint8Array;
  text: ReadonlyUint8Array;
  deadlineSecs: bigint;
  size: bigint;
};

export type FulfillSelfPromiseInstructionDataArgs = {
  text: ReadonlyUint8Array;
  deadlineSecs: number | bigint;
  size: number | bigint;
};

export function getFulfillSelfPromiseInstructionDataEncoder(): FixedSizeEncoder<FulfillSelfPromiseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['text', fixEncoderSize(getBytesEncoder(), 8)],
      ['deadlineSecs', getU64Encoder()],
      ['size', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FULFILL_SELF_PROMISE_DISCRIMINATOR })
  );
}

export function getFulfillSelfPromiseInstructionDataDecoder(): FixedSizeDecoder<FulfillSelfPromiseInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['text', fixDecoderSize(getBytesDecoder(), 8)],
    ['deadlineSecs', getU64Decoder()],
    ['size', getU64Decoder()],
  ]);
}

export function getFulfillSelfPromiseInstructionDataCodec(): FixedSizeCodec<
  FulfillSelfPromiseInstructionDataArgs,
  FulfillSelfPromiseInstructionData
> {
  return combineCodec(
    getFulfillSelfPromiseInstructionDataEncoder(),
    getFulfillSelfPromiseInstructionDataDecoder()
  );
}

export type FulfillSelfPromiseAsyncInput<
  TAccountSigner extends string = string,
  TAccountPromiseAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  promiseAccount?: Address<TAccountPromiseAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  text: FulfillSelfPromiseInstructionDataArgs['text'];
  deadlineSecs: FulfillSelfPromiseInstructionDataArgs['deadlineSecs'];
  size: FulfillSelfPromiseInstructionDataArgs['size'];
};

export async function getFulfillSelfPromiseInstructionAsync<
  TAccountSigner extends string,
  TAccountPromiseAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
>(
  input: FulfillSelfPromiseAsyncInput<
    TAccountSigner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  FulfillSelfPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROMISESPRIMITIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    promiseAccount: { value: input.promiseAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.promiseAccount.value) {
    accounts.promiseAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            115, 101, 108, 102, 112, 114, 111, 109, 105, 115, 101,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
        fixEncoderSize(getBytesEncoder(), 8).encode(expectSome(args.text)),
        getU64Encoder().encode(expectSome(args.deadlineSecs)),
        getU64Encoder().encode(expectSome(args.size)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.promiseAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFulfillSelfPromiseInstructionDataEncoder().encode(
      args as FulfillSelfPromiseInstructionDataArgs
    ),
    programAddress,
  } as FulfillSelfPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >);
}

export type FulfillSelfPromiseInput<
  TAccountSigner extends string = string,
  TAccountPromiseAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  promiseAccount: Address<TAccountPromiseAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  text: FulfillSelfPromiseInstructionDataArgs['text'];
  deadlineSecs: FulfillSelfPromiseInstructionDataArgs['deadlineSecs'];
  size: FulfillSelfPromiseInstructionDataArgs['size'];
};

export function getFulfillSelfPromiseInstruction<
  TAccountSigner extends string,
  TAccountPromiseAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
>(
  input: FulfillSelfPromiseInput<
    TAccountSigner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): FulfillSelfPromiseInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountPromiseAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROMISESPRIMITIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    promiseAccount: { value: input.promiseAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.promiseAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFulfillSelfPromiseInstructionDataEncoder().encode(
      args as FulfillSelfPromiseInstructionDataArgs
    ),
    programAddress,
  } as FulfillSelfPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >);
}

export type ParsedFulfillSelfPromiseInstruction<
  TProgram extends string = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    promiseAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: FulfillSelfPromiseInstructionData;
};

export function parseFulfillSelfPromiseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedFulfillSelfPromiseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      promiseAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFulfillSelfPromiseInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
