/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PROMISESPRIMITIVE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const FULFILL_PARTNER_PROMISE_DISCRIMINATOR = new Uint8Array([
  200, 27, 31, 148, 238, 107, 58, 173,
]);

export function getFulfillPartnerPromiseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FULFILL_PARTNER_PROMISE_DISCRIMINATOR
  );
}

export type FulfillPartnerPromiseInstruction<
  TProgram extends string = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountPromiseAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountPromiseAccount extends string
        ? WritableAccount<TAccountPromiseAccount>
        : TAccountPromiseAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FulfillPartnerPromiseInstructionData = {
  discriminator: ReadonlyUint8Array;
  text: ReadonlyUint8Array;
  deadlineSecs: bigint;
  size: bigint;
};

export type FulfillPartnerPromiseInstructionDataArgs = {
  text: ReadonlyUint8Array;
  deadlineSecs: number | bigint;
  size: number | bigint;
};

export function getFulfillPartnerPromiseInstructionDataEncoder(): FixedSizeEncoder<FulfillPartnerPromiseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['text', fixEncoderSize(getBytesEncoder(), 8)],
      ['deadlineSecs', getU64Encoder()],
      ['size', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: FULFILL_PARTNER_PROMISE_DISCRIMINATOR,
    })
  );
}

export function getFulfillPartnerPromiseInstructionDataDecoder(): FixedSizeDecoder<FulfillPartnerPromiseInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['text', fixDecoderSize(getBytesDecoder(), 8)],
    ['deadlineSecs', getU64Decoder()],
    ['size', getU64Decoder()],
  ]);
}

export function getFulfillPartnerPromiseInstructionDataCodec(): FixedSizeCodec<
  FulfillPartnerPromiseInstructionDataArgs,
  FulfillPartnerPromiseInstructionData
> {
  return combineCodec(
    getFulfillPartnerPromiseInstructionDataEncoder(),
    getFulfillPartnerPromiseInstructionDataDecoder()
  );
}

export type FulfillPartnerPromiseAsyncInput<
  TAccountSigner extends string = string,
  TAccountCreator extends string = string,
  TAccountPromiseAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  creator: Address<TAccountCreator>;
  promiseAccount?: Address<TAccountPromiseAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  text: FulfillPartnerPromiseInstructionDataArgs['text'];
  deadlineSecs: FulfillPartnerPromiseInstructionDataArgs['deadlineSecs'];
  size: FulfillPartnerPromiseInstructionDataArgs['size'];
};

export async function getFulfillPartnerPromiseInstructionAsync<
  TAccountSigner extends string,
  TAccountCreator extends string,
  TAccountPromiseAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
>(
  input: FulfillPartnerPromiseAsyncInput<
    TAccountSigner,
    TAccountCreator,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  FulfillPartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROMISESPRIMITIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    promiseAccount: { value: input.promiseAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.promiseAccount.value) {
    accounts.promiseAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 97, 114, 116, 110, 101, 114, 112, 114, 111, 109, 105, 115, 101,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.creator.value)),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
        fixEncoderSize(getBytesEncoder(), 8).encode(expectSome(args.text)),
        getU64Encoder().encode(expectSome(args.deadlineSecs)),
        getU64Encoder().encode(expectSome(args.size)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.promiseAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFulfillPartnerPromiseInstructionDataEncoder().encode(
      args as FulfillPartnerPromiseInstructionDataArgs
    ),
    programAddress,
  } as FulfillPartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >);
}

export type FulfillPartnerPromiseInput<
  TAccountSigner extends string = string,
  TAccountCreator extends string = string,
  TAccountPromiseAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  creator: Address<TAccountCreator>;
  promiseAccount: Address<TAccountPromiseAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  text: FulfillPartnerPromiseInstructionDataArgs['text'];
  deadlineSecs: FulfillPartnerPromiseInstructionDataArgs['deadlineSecs'];
  size: FulfillPartnerPromiseInstructionDataArgs['size'];
};

export function getFulfillPartnerPromiseInstruction<
  TAccountSigner extends string,
  TAccountCreator extends string,
  TAccountPromiseAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
>(
  input: FulfillPartnerPromiseInput<
    TAccountSigner,
    TAccountCreator,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): FulfillPartnerPromiseInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountCreator,
  TAccountPromiseAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROMISESPRIMITIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    promiseAccount: { value: input.promiseAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.promiseAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFulfillPartnerPromiseInstructionDataEncoder().encode(
      args as FulfillPartnerPromiseInstructionDataArgs
    ),
    programAddress,
  } as FulfillPartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >);
}

export type ParsedFulfillPartnerPromiseInstruction<
  TProgram extends string = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    creator: TAccountMetas[1];
    promiseAccount: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: FulfillPartnerPromiseInstructionData;
};

export function parseFulfillPartnerPromiseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedFulfillPartnerPromiseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      creator: getNextAccount(),
      promiseAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFulfillPartnerPromiseInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
