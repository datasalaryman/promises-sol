/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PROMISESPRIMITIVE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const BREAK_PARTNER_PROMISE_DISCRIMINATOR = new Uint8Array([
  231, 251, 137, 144, 185, 65, 135, 199,
]);

export function getBreakPartnerPromiseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BREAK_PARTNER_PROMISE_DISCRIMINATOR
  );
}

export type BreakPartnerPromiseInstruction<
  TProgram extends string = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
  TAccountSigner extends
    | string
    | AccountMeta<string> = 'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ',
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TAccountPromiseAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountPartner extends string
        ? WritableAccount<TAccountPartner>
        : TAccountPartner,
      TAccountPromiseAccount extends string
        ? WritableAccount<TAccountPromiseAccount>
        : TAccountPromiseAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BreakPartnerPromiseInstructionData = {
  discriminator: ReadonlyUint8Array;
  text: ReadonlyUint8Array;
  deadlineSecs: bigint;
  size: bigint;
};

export type BreakPartnerPromiseInstructionDataArgs = {
  text: ReadonlyUint8Array;
  deadlineSecs: number | bigint;
  size: number | bigint;
};

export function getBreakPartnerPromiseInstructionDataEncoder(): FixedSizeEncoder<BreakPartnerPromiseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['text', fixEncoderSize(getBytesEncoder(), 8)],
      ['deadlineSecs', getU64Encoder()],
      ['size', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: BREAK_PARTNER_PROMISE_DISCRIMINATOR,
    })
  );
}

export function getBreakPartnerPromiseInstructionDataDecoder(): FixedSizeDecoder<BreakPartnerPromiseInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['text', fixDecoderSize(getBytesDecoder(), 8)],
    ['deadlineSecs', getU64Decoder()],
    ['size', getU64Decoder()],
  ]);
}

export function getBreakPartnerPromiseInstructionDataCodec(): FixedSizeCodec<
  BreakPartnerPromiseInstructionDataArgs,
  BreakPartnerPromiseInstructionData
> {
  return combineCodec(
    getBreakPartnerPromiseInstructionDataEncoder(),
    getBreakPartnerPromiseInstructionDataDecoder()
  );
}

export type BreakPartnerPromiseAsyncInput<
  TAccountSigner extends string = string,
  TAccountCreator extends string = string,
  TAccountPartner extends string = string,
  TAccountPromiseAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer?: TransactionSigner<TAccountSigner>;
  creator: Address<TAccountCreator>;
  partner: Address<TAccountPartner>;
  promiseAccount?: Address<TAccountPromiseAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  text: BreakPartnerPromiseInstructionDataArgs['text'];
  deadlineSecs: BreakPartnerPromiseInstructionDataArgs['deadlineSecs'];
  size: BreakPartnerPromiseInstructionDataArgs['size'];
};

export async function getBreakPartnerPromiseInstructionAsync<
  TAccountSigner extends string,
  TAccountCreator extends string,
  TAccountPartner extends string,
  TAccountPromiseAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
>(
  input: BreakPartnerPromiseAsyncInput<
    TAccountSigner,
    TAccountCreator,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  BreakPartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROMISESPRIMITIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    partner: { value: input.partner ?? null, isWritable: true },
    promiseAccount: { value: input.promiseAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.signer.value) {
    accounts.signer.value =
      'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ' as Address<'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ'>;
  }
  if (!accounts.promiseAccount.value) {
    accounts.promiseAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 97, 114, 116, 110, 101, 114, 112, 114, 111, 109, 105, 115, 101,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.creator.value)),
        getAddressEncoder().encode(expectAddress(accounts.partner.value)),
        fixEncoderSize(getBytesEncoder(), 8).encode(expectSome(args.text)),
        getU64Encoder().encode(expectSome(args.deadlineSecs)),
        getU64Encoder().encode(expectSome(args.size)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.partner),
      getAccountMeta(accounts.promiseAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBreakPartnerPromiseInstructionDataEncoder().encode(
      args as BreakPartnerPromiseInstructionDataArgs
    ),
    programAddress,
  } as BreakPartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >);
}

export type BreakPartnerPromiseInput<
  TAccountSigner extends string = string,
  TAccountCreator extends string = string,
  TAccountPartner extends string = string,
  TAccountPromiseAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer?: TransactionSigner<TAccountSigner>;
  creator: Address<TAccountCreator>;
  partner: Address<TAccountPartner>;
  promiseAccount: Address<TAccountPromiseAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  text: BreakPartnerPromiseInstructionDataArgs['text'];
  deadlineSecs: BreakPartnerPromiseInstructionDataArgs['deadlineSecs'];
  size: BreakPartnerPromiseInstructionDataArgs['size'];
};

export function getBreakPartnerPromiseInstruction<
  TAccountSigner extends string,
  TAccountCreator extends string,
  TAccountPartner extends string,
  TAccountPromiseAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
>(
  input: BreakPartnerPromiseInput<
    TAccountSigner,
    TAccountCreator,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): BreakPartnerPromiseInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountCreator,
  TAccountPartner,
  TAccountPromiseAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROMISESPRIMITIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    partner: { value: input.partner ?? null, isWritable: true },
    promiseAccount: { value: input.promiseAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.signer.value) {
    accounts.signer.value =
      'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ' as Address<'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.partner),
      getAccountMeta(accounts.promiseAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBreakPartnerPromiseInstructionDataEncoder().encode(
      args as BreakPartnerPromiseInstructionDataArgs
    ),
    programAddress,
  } as BreakPartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountSystemProgram
  >);
}

export type ParsedBreakPartnerPromiseInstruction<
  TProgram extends string = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    creator: TAccountMetas[1];
    partner: TAccountMetas[2];
    promiseAccount: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: BreakPartnerPromiseInstructionData;
};

export function parseBreakPartnerPromiseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedBreakPartnerPromiseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      creator: getNextAccount(),
      partner: getNextAccount(),
      promiseAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBreakPartnerPromiseInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
