/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PROMISESPRIMITIVE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const MAKE_PARTNER_PROMISE_DISCRIMINATOR = new Uint8Array([
  231, 116, 7, 149, 225, 237, 135, 250,
]);

export function getMakePartnerPromiseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MAKE_PARTNER_PROMISE_DISCRIMINATOR
  );
}

export type MakePartnerPromiseInstruction<
  TProgram extends string = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountPartner extends string | AccountMeta<string> = string,
  TAccountPromiseAccount extends string | AccountMeta<string> = string,
  TAccountAuthor extends
    | string
    | AccountMeta<string> = 'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ',
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountPartner extends string
        ? WritableAccount<TAccountPartner>
        : TAccountPartner,
      TAccountPromiseAccount extends string
        ? WritableAccount<TAccountPromiseAccount>
        : TAccountPromiseAccount,
      TAccountAuthor extends string
        ? WritableAccount<TAccountAuthor>
        : TAccountAuthor,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MakePartnerPromiseInstructionData = {
  discriminator: ReadonlyUint8Array;
  text: ReadonlyUint8Array;
  deadlineSecs: bigint;
  size: bigint;
};

export type MakePartnerPromiseInstructionDataArgs = {
  text: ReadonlyUint8Array;
  deadlineSecs: number | bigint;
  size: number | bigint;
};

export function getMakePartnerPromiseInstructionDataEncoder(): FixedSizeEncoder<MakePartnerPromiseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['text', fixEncoderSize(getBytesEncoder(), 8)],
      ['deadlineSecs', getU64Encoder()],
      ['size', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MAKE_PARTNER_PROMISE_DISCRIMINATOR })
  );
}

export function getMakePartnerPromiseInstructionDataDecoder(): FixedSizeDecoder<MakePartnerPromiseInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['text', fixDecoderSize(getBytesDecoder(), 8)],
    ['deadlineSecs', getU64Decoder()],
    ['size', getU64Decoder()],
  ]);
}

export function getMakePartnerPromiseInstructionDataCodec(): FixedSizeCodec<
  MakePartnerPromiseInstructionDataArgs,
  MakePartnerPromiseInstructionData
> {
  return combineCodec(
    getMakePartnerPromiseInstructionDataEncoder(),
    getMakePartnerPromiseInstructionDataDecoder()
  );
}

export type MakePartnerPromiseAsyncInput<
  TAccountSigner extends string = string,
  TAccountPartner extends string = string,
  TAccountPromiseAccount extends string = string,
  TAccountAuthor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  partner: Address<TAccountPartner>;
  promiseAccount?: Address<TAccountPromiseAccount>;
  author?: Address<TAccountAuthor>;
  systemProgram?: Address<TAccountSystemProgram>;
  text: MakePartnerPromiseInstructionDataArgs['text'];
  deadlineSecs: MakePartnerPromiseInstructionDataArgs['deadlineSecs'];
  size: MakePartnerPromiseInstructionDataArgs['size'];
};

export async function getMakePartnerPromiseInstructionAsync<
  TAccountSigner extends string,
  TAccountPartner extends string,
  TAccountPromiseAccount extends string,
  TAccountAuthor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
>(
  input: MakePartnerPromiseAsyncInput<
    TAccountSigner,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountAuthor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  MakePartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountAuthor,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROMISESPRIMITIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    partner: { value: input.partner ?? null, isWritable: true },
    promiseAccount: { value: input.promiseAccount ?? null, isWritable: true },
    author: { value: input.author ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.promiseAccount.value) {
    accounts.promiseAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 97, 114, 116, 110, 101, 114, 112, 114, 111, 109, 105, 115, 101,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
        getAddressEncoder().encode(expectAddress(accounts.partner.value)),
        fixEncoderSize(getBytesEncoder(), 8).encode(expectSome(args.text)),
        getU64Encoder().encode(expectSome(args.deadlineSecs)),
        getU64Encoder().encode(expectSome(args.size)),
      ],
    });
  }
  if (!accounts.author.value) {
    accounts.author.value =
      'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ' as Address<'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.partner),
      getAccountMeta(accounts.promiseAccount),
      getAccountMeta(accounts.author),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getMakePartnerPromiseInstructionDataEncoder().encode(
      args as MakePartnerPromiseInstructionDataArgs
    ),
    programAddress,
  } as MakePartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountAuthor,
    TAccountSystemProgram
  >);
}

export type MakePartnerPromiseInput<
  TAccountSigner extends string = string,
  TAccountPartner extends string = string,
  TAccountPromiseAccount extends string = string,
  TAccountAuthor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  partner: Address<TAccountPartner>;
  promiseAccount: Address<TAccountPromiseAccount>;
  author?: Address<TAccountAuthor>;
  systemProgram?: Address<TAccountSystemProgram>;
  text: MakePartnerPromiseInstructionDataArgs['text'];
  deadlineSecs: MakePartnerPromiseInstructionDataArgs['deadlineSecs'];
  size: MakePartnerPromiseInstructionDataArgs['size'];
};

export function getMakePartnerPromiseInstruction<
  TAccountSigner extends string,
  TAccountPartner extends string,
  TAccountPromiseAccount extends string,
  TAccountAuthor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
>(
  input: MakePartnerPromiseInput<
    TAccountSigner,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountAuthor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): MakePartnerPromiseInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountPartner,
  TAccountPromiseAccount,
  TAccountAuthor,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROMISESPRIMITIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    partner: { value: input.partner ?? null, isWritable: true },
    promiseAccount: { value: input.promiseAccount ?? null, isWritable: true },
    author: { value: input.author ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.author.value) {
    accounts.author.value =
      'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ' as Address<'fTcVudr5vjBanSe9eYuX9HS3DuzjWKwavYBMbhLn2SJ'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.partner),
      getAccountMeta(accounts.promiseAccount),
      getAccountMeta(accounts.author),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getMakePartnerPromiseInstructionDataEncoder().encode(
      args as MakePartnerPromiseInstructionDataArgs
    ),
    programAddress,
  } as MakePartnerPromiseInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPartner,
    TAccountPromiseAccount,
    TAccountAuthor,
    TAccountSystemProgram
  >);
}

export type ParsedMakePartnerPromiseInstruction<
  TProgram extends string = typeof PROMISESPRIMITIVE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    partner: TAccountMetas[1];
    promiseAccount: TAccountMetas[2];
    author: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: MakePartnerPromiseInstructionData;
};

export function parseMakePartnerPromiseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedMakePartnerPromiseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      partner: getNextAccount(),
      promiseAccount: getNextAccount(),
      author: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMakePartnerPromiseInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
