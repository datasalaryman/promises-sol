/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const PARTNER_PROMISE_DISCRIMINATOR = new Uint8Array([
  60, 254, 165, 64, 158, 134, 176, 187,
]);

export function getPartnerPromiseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PARTNER_PROMISE_DISCRIMINATOR
  );
}

export type PartnerPromise = {
  discriminator: ReadonlyUint8Array;
  partner: Address;
  text: ReadonlyUint8Array;
  unixSeconds: bigint;
  size: bigint;
  bump: number;
};

export type PartnerPromiseArgs = {
  partner: Address;
  text: ReadonlyUint8Array;
  unixSeconds: number | bigint;
  size: number | bigint;
  bump: number;
};

export function getPartnerPromiseEncoder(): FixedSizeEncoder<PartnerPromiseArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['partner', getAddressEncoder()],
      ['text', fixEncoderSize(getBytesEncoder(), 8)],
      ['unixSeconds', getU64Encoder()],
      ['size', getU64Encoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PARTNER_PROMISE_DISCRIMINATOR })
  );
}

export function getPartnerPromiseDecoder(): FixedSizeDecoder<PartnerPromise> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['partner', getAddressDecoder()],
    ['text', fixDecoderSize(getBytesDecoder(), 8)],
    ['unixSeconds', getU64Decoder()],
    ['size', getU64Decoder()],
    ['bump', getU8Decoder()],
  ]);
}

export function getPartnerPromiseCodec(): FixedSizeCodec<
  PartnerPromiseArgs,
  PartnerPromise
> {
  return combineCodec(getPartnerPromiseEncoder(), getPartnerPromiseDecoder());
}

export function decodePartnerPromise<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<PartnerPromise, TAddress>;
export function decodePartnerPromise<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<PartnerPromise, TAddress>;
export function decodePartnerPromise<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<PartnerPromise, TAddress> | MaybeAccount<PartnerPromise, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPartnerPromiseDecoder()
  );
}

export async function fetchPartnerPromise<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<PartnerPromise, TAddress>> {
  const maybeAccount = await fetchMaybePartnerPromise(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePartnerPromise<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<PartnerPromise, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePartnerPromise(maybeAccount);
}

export async function fetchAllPartnerPromise(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<PartnerPromise>[]> {
  const maybeAccounts = await fetchAllMaybePartnerPromise(
    rpc,
    addresses,
    config
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePartnerPromise(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<PartnerPromise>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodePartnerPromise(maybeAccount)
  );
}

export function getPartnerPromiseSize(): number {
  return 65;
}
